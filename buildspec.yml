version: 0.2

phases:
  pre_build:
    commands:
      - chmod +x ./gradlew
  build:
    commands:
      - ./gradlew build
      - ./gradlew test
  post_build:
    commands:
      - '[ "${CODEBUILD_BUILD_SUCCEEDING}" -ne 1 ] && exit 1'
      - echo "Build suceeded!"
